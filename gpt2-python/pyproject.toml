[project]
name = "gpt2-python"
version = "0.1.0"
description = "Minimal GPT-2 implementation in PyTorch"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.0.0",
    "numpy>=1.24.0",
]
